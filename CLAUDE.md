# Purpose
- I am building a compile-time clojurescript i18n library using macros to capture errors & statistics up front.
- `src/` is used for library code, and `test` contains a simple shadow-cljs repo for testing my library.

# Version Control
- This repo uses Jujutsu.
