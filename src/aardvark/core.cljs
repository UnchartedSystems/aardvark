(ns aardvark.core)

;; Plan
;; Ingest & parse client translations
;; Process all phrases across the app
;; Process all of the langs for switching
;; Cross-check translations, phrases, and langs and process errors + stats
;; Replace phrases and langs with efficient run-time translations


(def marco "polo")
